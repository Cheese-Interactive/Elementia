using UnityEngine;

public class FakeHotbar : MonoBehaviour {
    //welcome to the worlds laziest code
    [SerializeField] private ItemSelectionIndicator[] indicators;

    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }
}
